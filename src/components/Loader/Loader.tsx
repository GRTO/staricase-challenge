import { FC } from "react";
import { LoaderContainer, LoaderElement } from "./LoaderStyles";

const Loader: FC = () => (
  <LoaderContainer>
    <LoaderElement />
  </LoaderContainer>
);

export default Loader;
